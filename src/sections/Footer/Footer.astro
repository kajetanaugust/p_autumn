---
import { Facebook } from 'lucide-astro'

import Box from '@components/Box/Box.astro'
import Button from '@components/Button/Button.astro'
import Text from '@components/Text/Text.astro'

import { footerWrapper } from './styles.css'

const currentYear = new Date().getFullYear()
---

<Box
  width="100%"
  direction={['column', 'row']}
  px={[16, 64]}
  py={20}
  align="center"
  justify="center"
  class={footerWrapper}
  gap={24}
>
  <Box direction="row" justify="space-between" align="center" maxWidth={1920}>
    <Text variant="text-small">©{currentYear} Pogodna Jesień Sp.z o o.</Text>
    <Text variant="text-small" textAlign="center"
      >Zezwolenie Wojewody Śląskiego na prowadzenie całodobowej opieki nad
      osobami niepełnosprawnymi i starszymi nr: PS.II.9041/2/10
    </Text>
    <Button
      variant="iconGhost"
      href="https://www.facebook.com/pensjonatpogodnajesien"
      aria-label="facebook link"
    >
      <Facebook />
    </Button>
  </Box>
</Box>
